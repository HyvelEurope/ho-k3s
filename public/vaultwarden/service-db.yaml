apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: vw
  labels:
    app.kubernetes.io/name: vaultwarden
    app.kubernetes.io/component: db
spec:
  selector:
    app.kubernetes.io/name: vaultwarden
    app.kubernetes.io/component: db
  ports:
    - port: 5432
      protocol: TCP
  clusterIP: None
