apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
    name: reflector
    namespace: reflector
spec:
    interval: 10m
    chart:
        spec:
            chart: reflector
            version: 10.0.45
            sourceRef:
                kind: HelmRepository
                name: emberstack
                namespace: flux-system
