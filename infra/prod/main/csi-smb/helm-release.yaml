apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
    name: csi-smb
    namespace: csi-smb
spec:
    interval: 10m
    chart:
        spec:
            chart: csi-driver-smb
            version: 1.20.3
            sourceRef:
                kind: HelmRepository
                name: csi-smb
                namespace: flux-system
