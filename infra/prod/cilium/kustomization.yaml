apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - helm-repo.yaml
  - helm-release.yaml
  - bgp-adv.yaml
  - bgp-cluster-config.yaml
  - bgp-peer-config.yaml
  - lb-pool.yaml
