apiVersion: v1
kind: Service
metadata:
  name: app
  namespace: anubis
  labels:
    app.kubernetes.io/name: anubis
    app.kubernetes.io/component: app
spec:
  selector:
    app.kubernetes.io/name: anubis
    app.kubernetes.io/component: app
  ports:
    - port: 80
      protocol: TCP
  type: ClusterIP
