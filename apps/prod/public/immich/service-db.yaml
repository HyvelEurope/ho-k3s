apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: immich
  labels:
    app.kubernetes.io/name: immich
    app.kubernetes.io/component: db
spec:
  selector:
    app.kubernetes.io/name: immich
    app.kubernetes.io/component: db
  ports:
    - port: 5432
  clusterIP: None

---

apiVersion: v1
kind: Service
metadata:
  name: cache
  namespace: immich
  labels:
    app.kubernetes.io/name: immich
    app.kubernetes.io/component: cache
spec:
  selector:
    app.kubernetes.io/name: immich
    app.kubernetes.io/component: cache
  ports:
    - port: 6379
      protocol: TCP
  clusterIP: None